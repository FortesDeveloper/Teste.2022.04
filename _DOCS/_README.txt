A Aplicação foi desenvolvida usando as ferramentas:
- Delhi 10.3.3
- Jedi VCL
- Banco SqlServer 2019

- Não foi aplicado certos conceitos como:
Classe, Herança, Polimorfismo, etc., por se tratar de um teste apenas. 
É óbvio que em uma aplicação real seria criado objetos e bibliotecas dedicadas para cada tarefa no sistema, assim como as heranças tanto de código quanto visuais e separação bem como a organização das pastas de projeto(Estrutura).


ATENÇÃO
------------
ANTES de EXECUTAR o projeto deve se alterar as propriedades de conexao do mesmo
configuração de conexão =>  conn.txt

Aqui também resalto que o mesmo não se aplica em aplicação real, por motivo de segurança.